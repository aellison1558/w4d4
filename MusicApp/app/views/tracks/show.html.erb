<h1><%= @track.name %></h1>
<%= link_to "Edit Track", edit_track_url(@track) %>
<%= link_to "Back to #{@album.name}", album_url(@album) %>

<p>Track Type:</p>
<p> <%= h(@track.track_type) %></p>

<p>Lyrics:</p>
<p><%= h(@track.lyrics) %></p>

<%= button_to "delete track", track_url(@track), action: 'delete', method: :delete %>
