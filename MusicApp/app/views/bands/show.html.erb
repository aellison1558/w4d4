<h1><%= @band.name %></h1>
<%= link_to "Edit Band", edit_band_url %>
<br>
<%= link_to "Back to Bands", bands_url %>

<h3>Albums</h3>
<% @band.albums.each do |album| %>
  <p> <%= link_to "#{album.name}", album_url(album) %></p>
<% end %>
<%= link_to "Add Album", new_band_album_url(@band) %>

<%= button_to "delete band", band_url(@band), action: 'delete', method: :delete %>
